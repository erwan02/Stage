<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title><%= title %></title>
  <link rel="stylesheet" href="css/publicationArticle.css" />
  <link rel="icon" href="images/Logo1.png">
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/headerNav.css">
  <link rel="stylesheet" href="/css/footer.css">
   <style>
    /* Seulement si pas déjà présent dans ton CSS */
    .sr-only {
      position:absolute;width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;
      clip:rect(0 0 0 0);white-space:nowrap;border:0;
    }
    .zoom-controls { 
      display:inline-flex; 
      gap:6px; 
      margin-left:10px; 
    }
    .zoom-controls .bouton {
      padding: 8px 14px;
      border: none;
      background-color: #ffb700;
      color: #1f2937;
      border-radius: 6px;
      cursor: pointer;
      font-weight: 600;
      font-family: 'Inter', sans-serif;
      transition: background 0.3s, transform 0.2s;
    }
    .zoom-controls .bouton:hover {
      background-color: #F0A30A;
      transform: translateY(-2px);
    }
  </style>
</head>
<body>
  <div class="live-region" aria-live="polite" aria-atomic="true" id="announcements"></div>
  
  <%- include('partials/header') %>
  <%- include('partials/nav-bar') %>

  

  <main>
    <section class="BlocA">
      <h2>Formulaire de publication d'article</h2>
      <form action="#" method="post" class="Article">
        <label for="titre">Titre de l'article</label>
        <input type="text" id="titre" name="titre" placeholder="Titre de l'article" required>

        <label for="auteur">Auteur</label>
        <input type="text" id="auteur" name="auteur" placeholder="Nom de l'auteur" required>

        <label for="contenu">Contenu</label>
        <textarea id="contenu" name="contenu" placeholder="Rédigez votre article ici..." rows="10" required></textarea>

        <label for="image">Image de couverture (URL ou fichier)</label>
    <label class="drop-zone" for="fichier">
        <img src="https://cdn-icons-png.flaticon.com/512/1828/1828911.png" alt="upload icon">
<a href="#" onclick="document.getElementById('fichier').click(); return false;">Choisis un fichier</a>
    </label>



        <button type="submit" class="bouton">Publier</button>
      </form>
    </section>
  </main>
  
  <%- include('partials/footer') %>
  <script src="javascript/accessibility.js" defer></script>
</body>
</html>